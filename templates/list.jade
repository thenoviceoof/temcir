extends "base.jade"

block content
  h2 Unsorted tasks
  ul
    for task in unsorted_tasks
      li
        h3 #{ task['name'] }
        p #{ task['notes'] }
        p #{ task['time'] }

  h2 Sorted tasks
